---
description: Pending ERD/schema-doc sync after applied DB changes — do not update ERD until the user asks
alwaysApply: true
---

# Pending ERD update (do not apply until asked)

When the user later asks to update the ERD / [`04_DATABASE_SCHEMA_FOR_CURSOR.md`](04_DATABASE_SCHEMA_FOR_CURSOR.md), bring schema docs in line with **live Prisma** + migrations. Do **not** rewrite the ERD unprompted.

## Known drift to merge (accumulated)

### Applied migration — notification schedules JSON
- Migration: `prisma/migrations/20260720120000_app_settings_notification_schedules_json`
- Table `app_settings`: add column `notification_schedules_json` TEXT NULL
- Stores Admin Reminder Settings schedule (JSON)

### Applied migration — invoice bank details JSON
- Migration: `prisma/migrations/20260720100000_invoice_bank_details_json`
- Table `invoices`: add column `bank_details_json` TEXT NULL (`bankDetailsJson`)
- Snapshot of bank + signatories for digital Dizlee→OpCo invoices
- Note: may already exist on some local DBs from earlier `db push`; migration history now tracks it for TiDB/CD

### Applied migration — email template category
- Migration: `prisma/migrations/20260715100000_email_template_category`
- Table `notification_templates`: add column `category VARCHAR(32) NOT NULL DEFAULT 'OTHER'`
- Index: `idx_notification_templates_category`
- **Updated values (app convention):** `INTIMATION` | `REMINDER` | `OTHER` (was `REPORTS` | `INVOICES` | `OTHER`)
- Remap migration: `prisma/migrations/20260716170000_email_template_category_intimation_reminder`
- Seed/data: intimations (`REPORT_SUBMISSION`, `INVOICE_SUBMISSION`), reminders (`REPORT_REMINDER`, `INVOICE_REMINDER`), Other (`PASSWORD_INVITE`, `PASSWORD_FORGOT`)
- Dizlee broadcast picker uses `INTIMATION` + `REMINDER` (excludes Other/password)

### Other schema already in Prisma (verify ERD/doc vs `prisma/schema.prisma`)
- Lookup seed: `INVOICE_STATUS` includes **PAID** (Settled → Paid lifecycle)
- Soft-delete and audit patterns as in current Prisma models
- Auth password emails are **DB-backed templates** under Other (not “hardcoded only” in ERD notes)
- Reminder schedules live in `app_settings.notification_schedules_json` (JSON, no extra table)

### Applied migration — service partner maps scoped to OpCo
- Migration: `prisma/migrations/20260818120000_service_partner_maps_opco_id`
- Table `service_partner_maps`: add `opco_id BIGINT NOT NULL` → `opcos.id`
- Drop unique `uq_service_partner_maps_service_key`
- Add unique `uq_service_partner_maps_opco_service_key` (`opco_id`, `service_key`)
- Same service name may map to different partners on different OpCos

### Applied migration — revenue share reports (generated Excel)
- Migration: `prisma/migrations/20260824120000_revenue_share_reports`
- Table `revenue_share_reports`: one generated RS Excel per OpCo + month + year
- Columns: `opco_id`, `month`, `year`, `file_id`, `generated_at`, `generated_by_user_id`, soft-delete/audit
- Unique: `uq_revenue_share_reports` (`opco_id`, `year`, `month`)
- Index: `idx_revenue_share_reports_period` (`year`, `month`)
- Local/blob folder: `revenue-share` under object storage (`.uploads/revenue-share/` locally)

### Applied migration — revenue share report items (row snapshot)
- Migration: `prisma/migrations/20260826120000_revenue_share_report_items`
- Table `revenue_share_report_items`: snapshot rows for a generated RS report
- Columns: `revenue_share_report_id`, `partner_id` (nullable), `partner_name`, `service_name`, `opco_amount_usd`, `partner_amount_usd`, `regulatory_fee_percent`, `regulatory_fee_amount`, `net_revenue`, `revenue_share_percent`, `sort_order`
- No audit/soft-delete on items — hard delete + recreate on regenerate
- Indexes: `idx_rs_report_items_report_id`, `idx_rs_report_items_report_sort`

### Applied migration — notification metadata JSON
- Migration: `prisma/migrations/20260824153000_notification_metadata_json`
- Table `notifications`: add column `metadata_json` TEXT NULL (`metadataJson`)
- Structured CTA / grouping payload (OpCo report-upload consolidation, reupload request links)

### Applied migration — OpCo partner link requests
- Migration: `prisma/migrations/20260827120000_opco_partner_link_requests`
- Table `opco_partner_link_requests`: OpCo asks Admin to link partners (PENDING / APPROVED / REJECTED)
- Columns: `opco_id`, `requested_by_user_id`, `month`, `year`, `message`, `partner_names_json`, `status`, `decision_note`, `decided_by_user_id`, `decided_at`, timestamps
- Indexes: `idx_opco_partner_link_requests_status_created`, `idx_opco_partner_link_requests_opco_id`

### Applied migration — OpCo monthly report submissions
- Migration: `prisma/migrations/20260825140000_opco_report_submissions`
- Table `opco_report_submissions`: one raw monthly Excel per OpCo + month + year (`file_id`, `status_id` via REPORT_STATUS)
- Unique: `uq_opco_report_submissions` (`opco_id`, `year`, `month`)
- Table `opco_submission_change_requests`: submission-scoped reupload CR (Partner keeps `report_change_requests`)
- Table `reports`: add nullable `submission_id` → `opco_report_submissions.id`
- Backfill: one submission per existing OpCo-lane (version=1) period group

## Source of truth until ERD is refreshed
1. [`prisma/schema.prisma`](prisma/schema.prisma)
2. Files under [`prisma/migrations/`](prisma/migrations/)
3. [`prisma/seed-data/`](prisma/seed-data/) (lookups + notification templates)

When syncing ERD: update table DDL, relationship notes, seed inventories, and any “FIX APPLIED” / changelog rows for the above.
